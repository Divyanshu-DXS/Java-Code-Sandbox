spring:
  application:
    name: library_backend
  datasource:
    url: jdbc:h2:mem:Mydatabase
    username: admin
    password: admin123
  sql:
    init:
      mode: always
  jpa:
    hibernate:
      ddl-auto: none

  h2:
    console:
      enabled: true
      path: /h2


server:
  port: 9091